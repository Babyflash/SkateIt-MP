<view style="position: relative;">
  <!-- this is where map should go -->
  <view class="map">
  <button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
		disabled="{{disabled}}" bindtap="primary"> Click to check if map is clickable</button>
    I'm a fucking map!
    </view>


  <!-- holds the accordian down -->
  <view style="width: 100%; height: 75vh; padding: 0px;"></view>
  <accordian>
    <logic spotTypes='{{spotTypes}}'></logic>
  </accordian>
</view>
  
 
     
      
     

